# Backend URL for Vite dev proxy (must match backend PORT)
VITE_DEV_API_TARGET=http://localhost:5000

# Leave unset in local dev so requests use /api and the Vite proxy above.
# Setting this to a production URL bypasses the proxy and causes empty/stale catalog data.
# VITE_API_BASE_URL=
